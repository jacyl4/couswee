## 1. 新版效果图对齐

- [x] 1.1 将用户本轮新版效果图作为视觉验收基准，先对比当前 Svelte 页面与新版图的差异。
- [x] 1.2 检查 `web/src/routes/+page.svelte` 和 `web/src/routes/global.css`，列出需要删除或替换的表格化、泛后台化和旧文案元素。
- [x] 1.3 确认现有 `/api/accounts` 与 `/api/codex/usage` 可用字段：昵称、active 状态、剩余百分比、reset 时间、stale/error、last refresh。
- [x] 1.4 确认新增账号与删除账号现有接口是否存在；若不存在，实现阶段只补最小 API 或先保留明确的 UI 未完成提示。

## 2. 数据模型与界面状态

- [x] 2.1 重构或新增前端类型，用于合并账号记录、用量记录、选中状态和展示状态。
- [x] 2.2 实现剩余百分比 helper：优先使用 `5h_remaining` 与 `weekly_remaining`，并将数值限制在 0-100。
- [x] 2.3 实现 `ok`、`warn`、`danger` 阈值 helper，基于 5h/weekly 两项剩余值中的较低值判断。
- [x] 2.4 实现 `可用账号`、`建议切换`、`最后同步` 三个概览值的派生逻辑。
- [x] 2.5 实现 `搜索账号...` 过滤逻辑，按昵称或显示标识匹配。
- [x] 2.6 实现账号选中集合、单卡片勾选/取消勾选、搜索后保留选择状态和 `已选择 N 项` 计数。

## 3. Svelte 页面实现

- [x] 3.1 将页面框架替换为新版效果图结构：`couswee 账号切换监测`、副标题、右上搜索、三张概览卡、账号管理工具条、账号卡片列表、底部同步提示。
- [x] 3.2 实现账号管理工具条：齿轮图标、`账号管理`、展开/收起按钮、`新增账号`、`删除选中`、右侧选中计数。
- [x] 3.3 实现账号卡片左侧复选框；选中时显示勾选图标并给整张卡片增加绿色高亮边框。
- [x] 3.4 每张账号卡片渲染圆形首字母头像、昵称、状态徽标（`可用` / `接近用尽` / `冷却中`）和 `上次切换` 元数据。
- [x] 3.5 渲染 `5h limit:` 与 `Weekly limit:` 两行，包含进度条、`% left` 文本和 `resets ...` 文案。
- [x] 3.6 保留醒目的单账号 `切换` 按钮，继续调用现有切换接口，并在成功后刷新账号与用量数据。
- [x] 3.7 保留加载中、无账号、搜索无结果和 API 错误状态，且这些状态必须出现在新版页面框架内。

## 4. 视觉系统与响应式

- [x] 4.1 重建 `global.css` 的暗色玻璃拟态系统：深绿黑背景、径向柔光、半透明卡片、暖色文字、细边框、阴影和轻微发光。
- [x] 4.2 为绿色、黄色、红色状态分别定义头像、徽标、进度条、卡片边框和 `切换` 按钮样式。
- [x] 4.3 调整桌面端间距、宽度、卡片高度、字体大小和对齐方式，使 1672x941 左右比例下接近新版效果图。
- [x] 4.4 调整账号管理工具条的折叠页签、按钮组和右侧 `已选择 N 项` 对齐方式。
- [x] 4.5 添加窄屏响应式规则，保证账号卡片纵向堆叠后仍保留昵称、剩余百分比、reset 文案、复选框和切换按钮。

## 5. 验证

- [x] 5.1 运行前端类型/构建验证（`npm run build` 或项目等价命令），修复 Svelte 或 TypeScript 错误。
- [x] 5.2 如果新增或修改了账号新增/删除相关后端接口，运行对应 Go 测试；如果没有修改后端，在实施记录中说明后端行为未变。
- [x] 5.3 人工对照用户本轮新版效果图检查页面结构、颜色、卡片选中态、账号管理栏、按钮、剩余流量语义和底部同步提示。
- [x] 5.4 实现完成后重新运行 `openspec validate align-account-dashboard-ui --strict`。
